#!/bin/sh

cd /tmp

echo "Downloading /usr/lib/perl archive..."
htget -O http://lamp.sourceforge.net/experimental/usr-lib-perl.jbin.gz

echo "Decompressing archive..."
gzip -d usr-lib-perl.jbin.gz

echo "Decoding archive..."
macbin --decode usr-lib-perl.jbin /usr/lib/

echo "Done."


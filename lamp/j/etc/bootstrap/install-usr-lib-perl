#!/bin/sh

echo "Downloading /usr/lib/perl archive..."
htget http://lamp.sourceforge.net/experimental/usr-lib-perl.mbin.gz --save /tmp/usr-lib-perl.mbin.gz

echo "Decompressing archive..."
gzip -d /tmp/usr-lib-perl.mbin.gz

echo "Decoding archive..."
macbin --decode /tmp/usr-lib-perl.mbin /usr/lib/

echo "Done."

